{
  "name": "risk-game-replay-viewer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "hosted": "npm run build && node server/index.cjs --port=80",
    "hosted-dev": "concurrently \"vite\" \"node --watch server/index.cjs\""
  },
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "better-sqlite3": "^11.0.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  }
}
